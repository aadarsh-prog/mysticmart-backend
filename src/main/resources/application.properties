server.port=8080

spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/mysticmart?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:root@1}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.datasource.hikari.pool-name=MysticMartPool
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.keepalive-time=60000

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.open-in-view=false

# ---------------- JWT ----------------
app.jwt.secret=${JWT_SECRET:dK9mP2xQvR8nL5wY3tH7jF4bZ6cA1eG0sU+iO/NmXqWpVdCyTlBhMgJkErFuAsZ}
app.jwt.expiration-ms=900000
app.jwt.refresh-expiration-ms=604800000

# ---------------- CACHE ----------------
spring.cache.type=caffeine
spring.cache.caffeine.spec=maximumSize=1000,expireAfterWrite=10m

# ---------------- MAIL ----------------
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:aadarshsahu2000@gmail.com}
spring.mail.password=${MAIL_PASSWORD:atlr oktw uwol lgxk}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# ---------------- PAYMENT ----------------
razorpay.key.id=${RAZORPAY_KEY_ID:rzp_test_SW8z2zbqsAfxX4}
razorpay.key.secret=${RAZORPAY_KEY_SECRET:hLlP4FqKxppbTWvmYomRYrfL}

# ---------------- CORS ----------------
app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:3000,http://localhost:5173}

# ---------------- THREAD POOL ----------------
spring.task.execution.pool.core-size=10
spring.task.execution.pool.max-size=50
spring.task.execution.pool.queue-capacity=500
spring.task.execution.pool.keep-alive=60s

# ---------------- LOGGING ----------------
logging.level.root=WARN
logging.level.com.mysticmart=INFO

# ---------------- ACTUATOR ----------------
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=never

# ---------------- SWAGGER ----------------
springdoc.swagger-ui.enabled=false
springdoc.api-docs.enabled=false